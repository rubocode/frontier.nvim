-- LIGHT TEXT
-- JUL 14, 2025

local syntax = require("firewatch.palette.syntax")
local uno = require("firewatch.palette.uno")

local M = {
	Loud = uno.Uno1,
	Normal = syntax.Fg,
	Quiet = uno.Uno4,
}

return M
