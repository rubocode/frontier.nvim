-- FIREWATCH EDITOR SPELLING PROFILE
-- JUL 15, 2025

local style = require("highlights.style")
local syntax = require("firewatch.palette.syntax")
local uno = require("firewatch.palette.uno")
local duo = require("firewatch.palette.duo")

local M = {
	SpellBad = {},
	SpellCap = {},
	SpellLocal = {},
	SpellRare = {},
}

return M
