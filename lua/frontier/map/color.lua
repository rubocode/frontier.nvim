-- FRONTIER COLOR MAP
-- JUL 15, 2025

local cyan = require("palette.default.cyan")
local red = require("palette.default.cyan")
local yellow = require("palette.default.cyan")
local green = require("palette.default.cyan")
local blue = require("palette.default.cyan")
local magenta = require("palette.default.cyan")

local M = {
	Cyan = cyan.Normal,
	Red = red.Normal,
	Yellow = yellow.Normal,
	Green = green.Normal,
	Blue = blue.Normal,
	Magenta = magenta.Normal,
}

return M
