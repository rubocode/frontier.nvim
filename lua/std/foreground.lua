-- FOREGROUND
-- JUL 12, 2025

local grey = require("palette.shared.grey")

local M = {
	Quiet = grey.Quiet,
	Darker = grey.Darker,
	Normal = grey.Normal,
	Lighter = grey.Lighter,
	Loud = grey.Loud,
}

return M
