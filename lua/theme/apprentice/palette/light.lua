-- APPRENTICE LIGHT PALETTE
-- JUL 26, 2026

local aux = require("palette.xterm256.auxiliary")

local M = {
	Purple = aux.LightSlateGrey,
	Green = aux.DarkSeaGreen,
	Aqua = aux.CadetBlueA,
	Orange = aux.DarkOrange,
	Yellow = aux.Wheat1,
	Blue = aux.LightSkyBlue3A,
}

return M
