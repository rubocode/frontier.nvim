-- APPRENTICE DIFF ARCHETYPE
-- JUL 27, 2025

local diff = require("theme.apprentice.palette.diff")

local M = {
	Add = diff.Add,
	Change = diff.Change,
	Delete = diff.Delete,
	Text = diff.Text,
}

return M
