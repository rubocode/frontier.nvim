-- GREEN SPECTRUM
-- JUL 05, 2025

local aux = require("frontier.palette.xterm256.auxiliary")

local M = {
	Darker = aux.DarkGreen,
	Normal = aux.DarkSeaGreen4,
	Lighter = aux.DarkSeaGreen,
}

return M
