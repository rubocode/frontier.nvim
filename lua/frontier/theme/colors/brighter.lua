-- BRIGHT PASTEL COLORS
-- JUL 03, 2025

local aux = require("frontier.palette.xterm256.auxiliary")

local M = {
	Blue = aux.LightSkyBlue3A,
	Cyan = aux.CadetBlueA,
	Green = aux.DarkSeaGreen,
	Purple = aux.LightSlateGrey,
	Red = aux.DarkOrange,
	Yellow = aux.Gold3,
}

return M
