-- FRONTIER TEXT ARCHETYPE
-- JUL 22, 2025

local grey = require("palette.default.grey")

local M = {
	Quiet = grey.Quiet,
	Darker = grey.Darker,
	Normal = grey.Normal,
	Lighter = grey.Lighter,
	Loud = grey.Loud,
}

return M
