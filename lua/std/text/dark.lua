-- LIGHT TEXT
-- JUL 14, 2025

local grey = require("palette.shared.grey")
local black = require("palette.shared.black")

local M = {
	Loud = black.Almost,
	Normal = grey.Darkest,
	Quiet = grey.Darker,
}

return M
